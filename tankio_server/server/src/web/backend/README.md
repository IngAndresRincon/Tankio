# Legacy backend

This folder is kept for reference only.

Active service:
- `server/server.js`

Do not start or extend this backend unless we explicitly decide to migrate code from it into the main server.
